<center><br>
    <div class="jumbotron col-lg-10 col-lg-offset-1 light-green">
        <h1><%= @experiment.name %></h1>
        <hr>
        <%= line_chart Datapoint.pluck(:measured_at, :value), xtitle: "Time", ytitle: "Value", download: true %>
        <hr>
        <%= scatter_chart Datapoint.pluck(:measured_at, :value), xtitle: "Time", ytitle: "Value", download: true %>
        <%= link_to "Back to Experiment", experiment_path(@experiment), class: "btn btn-primary btn-lg button-orange" %></p>
    </div>

</center>

