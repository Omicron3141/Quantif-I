<%= form_with model: @user, local: true, html: {:class => "form-horizontal"} do |form| %>
  <% if @user.errors.any? %>
    <div id="error_explanation" class="alert alert-danger">
      <h2>
        <%= pluralize(@user.errors.count, "error") %> prohibited
        this user from being saved:
      </h2>
      <ul>
        <% @user.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <table>
    <tr class="field">
      <td><%= form.label :name,  :class => "control-label"  %></td>
      <td><%= form.text_field :name,  :class => "name" %></td>
    </tr>
   
    <tr class="field">
      <td><%= form.label :email,  :class => "control-label"  %></td>
      <td><%= form.email_field :email, :class => "email" %></td>
    </tr>

    <tr class="field">
      <td><%= form.label :password,  :class => "control-label"  %></td>
      <td><%= form.password_field :password, :class => "password" %></td>
    </tr>

    <tr class="field">
      <td><%= form.label :password_confirmation,  :class => "control-label"  %></td>
      <td><%= form.password_field :password_confirmation, :class => "password" %></td>
    </tr>
  </table>
  <%= form.submit "Confirm", class: "btn btn-lg btn-default btn-primary button-orange"%>
  <%= link_to cancel_text, cancel_path, class: "btn btn-primary btn-lg button-orange" %>
<% end %>
